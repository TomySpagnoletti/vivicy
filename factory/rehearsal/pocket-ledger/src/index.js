// Pocket Ledger - rehearsal fixture scaffold.
//
// This is intentionally a placeholder. The dev-loop agents fill in the real
// implementation (Ledger, Categorizer, Reporter, Exporter, CLI) per the issues
// extracted from .vivicy/canonical/**. Until then this module exports a version
// marker only, so the scaffold imports cleanly and `npm test` is green.

export const POCKET_LEDGER_VERSION = "0.0.0";
