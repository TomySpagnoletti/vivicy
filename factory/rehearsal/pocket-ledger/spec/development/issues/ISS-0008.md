# ISS-0008 - Dispatch CLI commands over the wired modules

## Summary

Implement the CLI: the single command-line entry point that wires the Ledger, Categorizer, Reporter, and Exporter together. It reads its command and arguments from process arguments only, dispatches the `add`, `list`, `report`, and `export` commands, sends normal output to standard output and errors to standard error, and exits non-zero on an unknown command or wrong argument count.

## Task Type

implementation

## Traceability

```text
issue_id: ISS-0008
graph_refs:
  - node:cli
  - edge:user->cli:issues_commands:process_arguments
  - edge:cli->ledger:adds_and_reads_expenses:module_call
requirement_ids:
  - REQ-CLI-001
  - REQ-CLI-002
  - REQ-CLI-003
  - REQ-ARCH-003
source_line_refs:
  - docs/canonical/01-architecture.md:21
  - docs/canonical/01-architecture.md:25
  - docs/canonical/06-cli.md:7
  - docs/canonical/06-cli.md:11
  - docs/canonical/06-cli.md:13-16
  - docs/canonical/06-cli.md:20
  - docs/canonical/06-cli.md:24
depends_on:
  - ISS-0001
  - ISS-0002
  - ISS-0003
  - ISS-0004
  - ISS-0005
  - ISS-0006
  - ISS-0007
spike_gates:
verification_gate_ids:
  - gate:test:cli-dispatch
```

## Scope

Build the CLI as a pure-JS dispatch function under `src/` that takes an argument vector (the command and its arguments) and the wired modules, and produces the command result. The CLI is the only module permitted to read from process arguments and to write to standard output; it drives the flow in which an expense enters through the Ledger, is classified by the Categorizer, is aggregated by the Reporter, and may be serialized by the Exporter.

Commands:

- `add <amountCents> <date> <description>`: adds an expense to the Ledger, categorizing it via the Categorizer, and prints the assigned id.
- `list`: prints all expenses in insertion order.
- `report <month>`: prints the monthly total and category breakdown for the given `YYYY-MM` month.
- `export`: prints the full Ledger as CSV to standard output.

Argument handling: the CLI reads its command and arguments from process arguments only. An unknown command, or a command with the wrong number of arguments, exits with a non-zero status and a single error line on standard error.

Output discipline: normal command output goes to standard output; error messages go to standard error. The CLI never prints partial output before an argument-validation error. The CLI holds no state across invocations beyond what the Ledger persists.

## Out Of Scope

The behavior of the underlying modules (built in ISS-0001 through ISS-0007); this issue only wires and dispatches over them. It does not add new business rules to the Ledger, Categorizer, Reporter, or Exporter.

## Verification

Unit tests in `test/` (node:test) to full branch coverage, driving the dispatch function with an injected argument vector and capturing the stdout/stderr/exit result: `add` with valid arguments stores the expense (categorized) and emits the assigned id; `list` emits stored expenses in insertion order; `report` emits the monthly total and category breakdown for the requested month; `export` emits the full CSV; an unknown command exits non-zero with a single error line on stderr; a known command with the wrong argument count exits non-zero with a single error line on stderr; an argument-validation failure emits no partial standard output before the error. Tests assert the exact streams and exit status. The deterministic gate `gate:test:cli-dispatch` must be green (node --test exit 0) before this issue is reported complete.
