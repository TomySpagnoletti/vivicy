// Formula engine - rehearsal fixture scaffold.
//
// This is intentionally a placeholder. The dev-loop agents fill in the real
// implementation (tokenizer, parser, evaluator, sheet model, recalculation,
// error model, and the core/math/logical/text/lookup function families) per the
// issues extracted from .vivicy/canonical/**. Until then this module exports a
// version marker only, so the scaffold imports cleanly and `npm test` is green.

export const FORMULA_VERSION = "0.0.0";
