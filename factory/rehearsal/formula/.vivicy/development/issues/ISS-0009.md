# ISS-0009 - Text Functions

## Summary

This issue registers the text function family on the Registry. It implements CONCAT joining its coerced arguments in order, LEN counting characters after coercion, UPPER and LOWER case conversion, TRIM trimming and collapsing internal spaces, LEFT and RIGHT substring extraction with the zero-count and over-length bounds, and MID one-based substring extraction. A negative count (and, for MID, a start below one) yields a `#VALUE!` error value.

## Task Type

implementation

## Traceability

```text
issue_id: ISS-0009
graph_refs:
  - node:fn-text
requirement_ids:
  - REQ-FNTEXT-001
  - REQ-FNTEXT-002
  - REQ-FNTEXT-003
  - REQ-FNTEXT-004
source_line_refs:
  - .vivicy/canonical/11-functions-text.md:5-7
  - .vivicy/canonical/11-functions-text.md:9-11
  - .vivicy/canonical/11-functions-text.md:13-15
  - .vivicy/canonical/11-functions-text.md:17-19
depends_on:
  - ISS-0006
spike_gates:
verification_gate_ids:
  - gate:test:functions-text
```

## Scope

Register the text functions on the Registry using the shared calling convention and text coercion from ISS-0006. `CONCAT` joins all of its arguments into one string in argument order, coercing numbers and booleans to their text form and treating an empty cell value as the empty string. `LEN(text)` returns the number of characters in its single text argument after coercion.

Implement case and trimming. `UPPER(text)` returns its argument with every letter in upper case and `LOWER(text)` returns it with every letter in lower case. `TRIM(text)` removes leading and trailing spaces and collapses each run of internal spaces to a single space.

Implement the substring functions. `LEFT(text, count)` returns the first `count` characters and `RIGHT(text, count)` returns the last `count` characters, where a `count` of zero returns the empty string and a `count` larger than the length returns the whole string; a negative `count` yields a `#VALUE!` error value. `MID(text, start, count)` returns `count` characters beginning at the one-based `start` position, returning the empty string when `start` is past the end of the text and fewer characters when the text ends first; a `start` below one or a negative `count` yields a `#VALUE!` error value.

## Out Of Scope

The Registry, calling convention, default error propagation, and coercion utilities (owned by ISS-0006, consumed here). The math, logical, and lookup families (their own issues). Evaluating `call` nodes and dispatching them (owned by the Evaluator issue). This issue registers the text functions; it does not alter the convention.

## Verification

- Unit tests covering this issue's governed code to the Code Coverage Gate (100% lines, statements, branches, functions), asserting: CONCAT joining strings, numbers, booleans, and empty cells in argument order; LEN counting characters after coercion; UPPER/LOWER case conversion; TRIM removing leading/trailing spaces and collapsing internal runs to a single space; LEFT/RIGHT for counts of zero, within length, and larger than the length; and MID with a start inside the text, a start past the end (empty string), and a count running past the end (fewer characters).
- Negative and edge cases: a negative count for LEFT, RIGHT, and MID, and a start below one for MID, each yield `#VALUE!`.
- The deterministic gate `gate:test:functions-text` resolving to a real green gate-run record (status: pass, exit_code: 0), not a free-text label.

No later code or issue may build on this slice until it is proven fully functional and fully tested here, and completion is claimed only from fresh green evidence on the current tree.
