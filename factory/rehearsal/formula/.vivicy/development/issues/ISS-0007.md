# ISS-0007 - Math Functions

## Summary

This issue registers the math function family on the Registry. It implements the aggregation functions SUM, AVERAGE, MIN, MAX, and COUNT with their defined empty-input results, ROUND with round-half-away-from-zero and negative-digit rounding, ABS, MOD carrying the divisor's sign with a `#DIV/0!` on a zero divisor, and POWER returning `#VALUE!` when the result is not a finite real number. Aggregation functions flatten ranges in range order, skip empty cells, and coerce booleans and numeric strings, while COUNT counts only values that are already numbers.

## Task Type

implementation

## Traceability

```text
issue_id: ISS-0007
graph_refs:
  - node:fn-math
requirement_ids:
  - REQ-FNMATH-001
  - REQ-FNMATH-002
  - REQ-FNMATH-003
  - REQ-FNMATH-004
source_line_refs:
  - .vivicy/canonical/09-functions-math.md:5-7
  - .vivicy/canonical/09-functions-math.md:9-11
  - .vivicy/canonical/09-functions-math.md:13-15
  - .vivicy/canonical/09-functions-math.md:17-19
depends_on:
  - ISS-0006
spike_gates:
verification_gate_ids:
  - gate:test:functions-math
```

## Scope

Register the aggregation functions on the Registry using the shared calling convention and coercion/flattening utilities from ISS-0006. `SUM` returns the sum of its numeric arguments, `AVERAGE` returns their arithmetic mean, `MIN` returns the smallest and `MAX` the largest, and `COUNT` returns how many arguments are already numbers, before any coercion. `SUM`, `MIN`, and `MAX` over no numeric values return `0`, while `AVERAGE` over no numeric values returns a `#DIV/0!` error value. Aggregation functions accept any mix of scalar arguments and ranges, flatten ranges in range order, skip empty cell values, and coerce booleans and numeric strings to numbers; `COUNT` counts only values that are already numbers, applying no coercion, and never counts empty cells, and a non-coercible value yields a `#VALUE!` error value for `SUM`, `AVERAGE`, `MIN`, and `MAX`.

Register the rounding and sign functions. `ROUND(number, digits)` rounds the number to the given number of decimal digits using round-half-away-from-zero, where a negative `digits` rounds to the left of the decimal point. `ABS(number)` returns the absolute value of its single numeric argument.

Register the modulo and power functions. `MOD(number, divisor)` returns the remainder of `number` divided by `divisor` and carries the sign of the divisor, and a divisor of zero yields a `#DIV/0!` error value. `POWER(base, exponent)` raises `base` to `exponent` and yields a `#VALUE!` error value when the result is not a finite real number.

## Out Of Scope

The Registry, the shared calling convention, default error propagation, coercion, and range flattening (owned by ISS-0006, consumed here). The logical, text, and lookup families (their own issues). Evaluating `call` nodes and dispatching them (owned by the Evaluator issue). This issue registers the math functions; it does not change the calling convention they obey.

## Verification

- Unit tests covering this issue's governed code to the Code Coverage Gate (100% lines, statements, branches, functions), asserting: SUM/AVERAGE/MIN/MAX over scalars and ranges with empty-cell skipping and boolean/numeric-string coercion; COUNT counting only already-number values with no coercion and never counting empty cells; the empty-input results (SUM/MIN/MAX → 0, AVERAGE → `#DIV/0!`); ROUND half-away-from-zero for positive, zero, and negative digits; ABS of positive, negative, and zero; MOD carrying the divisor's sign; and POWER for ordinary, fractional, and non-finite-real results.
- Negative and edge cases: a non-coercible value yields `#VALUE!` for SUM/AVERAGE/MIN/MAX, MOD by a zero divisor yields `#DIV/0!`, and POWER yielding `#VALUE!` for a non-finite-real result (e.g. a negative base to a fractional exponent).
- The deterministic gate `gate:test:functions-math` resolving to a real green gate-run record (status: pass, exit_code: 0), not a free-text label.

No later code or issue may build on this slice until it is proven fully functional and fully tested here, and completion is claimed only from fresh green evidence on the current tree.
