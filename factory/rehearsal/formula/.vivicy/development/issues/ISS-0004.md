# ISS-0004 - Tokenizer Lexical Analysis

## Summary

This issue implements the Tokenizer, which turns a formula string into a flat, ordered list of tokens by lexical analysis only — it recognizes token kinds but never interprets precedence or structure. It emits number, string, ref, name, operator, lparen, rparen, comma, and eof tokens, handling numeric and double-quoted string literals (with doubled-quote escaping), A1 cell references and ranges, function-name identifiers, and skipped whitespace. Any character that cannot begin a valid token makes the Tokenizer report a `#NAME?` error result rather than throwing.

## Task Type

implementation

## Traceability

```text
issue_id: ISS-0004
graph_refs:
  - node:tokenizer
requirement_ids:
  - REQ-TOK-001
  - REQ-TOK-002
  - REQ-TOK-003
  - REQ-TOK-004
  - REQ-TOK-005
source_line_refs:
  - .vivicy/canonical/02-tokenizer.md:5-7
  - .vivicy/canonical/02-tokenizer.md:9-11
  - .vivicy/canonical/02-tokenizer.md:13-15
  - .vivicy/canonical/02-tokenizer.md:17-19
  - .vivicy/canonical/02-tokenizer.md:21-23
depends_on:
  - ISS-0001
spike_gates:
verification_gate_ids:
  - gate:test:tokenizer
```

## Scope

Implement a tokenizer that consumes a formula string and produces a flat, ordered token list. It performs lexical analysis only and never interprets precedence or structure. It emits these token kinds: `number` for numeric literals, `string` for double-quoted string literals, `ref` for an A1 cell reference or range, `name` for a bare identifier such as a function name, `operator` for one of the arithmetic or comparison operators, `lparen` and `rparen` for parentheses, `comma` for an argument separator, and `eof` at the end of input.

A `number` token matches an optional integer part, an optional fractional part introduced by a decimal point, and an optional exponent; its lexeme is converted to a JavaScript number. A `string` token is delimited by double quotes, and a doubled double quote inside the literal encodes a single literal double quote. A `ref` token matches an A1 cell reference such as `A1` or `BC12`, optionally followed by a colon and a second A1 reference to form a range such as `A1:B3`. A `name` token matches a letter followed by letters or digits and is used for function names; a `name` is produced only when the identifier is not a valid A1 reference. Spaces and tabs between tokens are skipped and never produce a token. Any character that cannot begin a valid token makes the Tokenizer report a `#NAME?` error result (using the error sentinel from ISS-0001) rather than throwing.

## Out Of Scope

Building an AST, operator precedence, and associativity (owned by the Parser issue). Evaluating tokens (owned by the Evaluator issue). Validating that a `ref` lexeme addresses a cell that exists (owned by reference resolution in the Evaluator and Sheet issues). This issue classifies lexemes; it does not assemble or evaluate them.

## Verification

- Unit tests covering this issue's governed code to the Code Coverage Gate (100% lines, statements, branches, functions), asserting: each token kind is emitted for representative input, numbers with integer/fractional/exponent forms convert to the correct JavaScript number, string literals decode doubled double quotes to a single quote, `ref` tokens are produced for single cells and ranges, `name` is produced for function-name identifiers but a valid A1 lexeme becomes `ref` rather than `name`, whitespace between tokens produces no token, and an `eof` token terminates the list.
- Negative and edge cases: an unlexable character returns a `#NAME?` error result rather than throwing, and empty input yields only `eof`.
- The deterministic gate `gate:test:tokenizer` resolving to a real green gate-run record (status: pass, exit_code: 0), not a free-text label.

No later code or issue may build on this slice until it is proven fully functional and fully tested here, and completion is claimed only from fresh green evidence on the current tree.
