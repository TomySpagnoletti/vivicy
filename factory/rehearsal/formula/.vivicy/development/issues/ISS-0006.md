# ISS-0006 - Function Registry and Calling Convention

## Summary

This issue implements the function Registry and the shared calling convention every function obeys. The Registry maps an upper-cased function name to a function implementation with case-insensitive lookup, resolving an unknown name to nothing so the Evaluator can raise `#NAME?`. The calling convention defines how a function receives its already-evaluated arguments (error values included), the default first-error propagation rule, the error-aware and error-skipping overrides, the number/text coercion rules, and how a range argument is flattened into a rectangular block.

## Task Type

implementation

## Traceability

```text
issue_id: ISS-0006
graph_refs:
  - node:registry
requirement_ids:
  - REQ-REG-001
  - REQ-REG-002
  - REQ-REG-003
  - REQ-REG-004
source_line_refs:
  - .vivicy/canonical/08-functions-core.md:5-7
  - .vivicy/canonical/08-functions-core.md:9-11
  - .vivicy/canonical/08-functions-core.md:13-15
  - .vivicy/canonical/08-functions-core.md:17-19
depends_on:
  - ISS-0001
spike_gates:
verification_gate_ids:
  - gate:test:registry
```

## Scope

Implement the function Registry: it maps an upper-cased function name to a function implementation. Lookup is case-insensitive, so `sum`, `Sum`, and `SUM` resolve to the same implementation, and a name with no registered implementation resolves to nothing so the Evaluator can raise `#NAME?`.

Implement the shared calling convention. Every registered function receives its already-evaluated arguments as an ordered list, error values included, and returns a single value. By default an error argument is resolved by propagation before any coercion or truthiness classification: the function returns the first error value among its arguments (using the first-error helper from ISS-0001), and its coercion and type rules then apply only to the remaining non-error values. Two kinds of function override this default: error-aware functions inspect an error argument to decide their result — `IF` (only an error condition propagates), `VLOOKUP` (only an error key propagates), and the error predicate and error-code reader — and error-skipping functions filter error values out instead of propagating them — `COUNT`, which counts only its numeric arguments. A function never reads the Sheet directly and never throws for a domain error: it returns one of the typed error values instead.

Implement argument coercion as a reusable utility the function families consume: a function that expects a number coerces a boolean to `1` for true and `0` for false, parses a numeric string, and treats an empty cell value as `0`; a value that cannot be coerced yields a `#VALUE!` error value. A function that expects text coerces a number or boolean to its text form. Implement range flattening: when an argument is a range, the function receives the range's rectangular block of cell values; an aggregating function reduces that block over its values in row-by-row order and skips empty cell values, a wrong-type value inside a range yields a `#VALUE!` error value for functions that compute over numbers except a pure counting function (which skips any value that is not a number rather than erroring), and a lookup function instead reads the block by its rows and columns.

## Out Of Scope

The concrete math, logical, text, and lookup function implementations (owned by ISS-0007 through ISS-0010, which register into this Registry and consume this convention). Evaluating AST `call` nodes and dispatching them against the Registry (owned by the Evaluator issue). Defining the error sentinels and the default first-error helper (owned by ISS-0001, consumed here). This issue delivers the Registry plus the shared convention, coercion, and flattening utilities — not the individual functions, and not any single function's domain behavior beyond the shared convention.

## Verification

- Unit tests covering this issue's governed code to the Code Coverage Gate (100% lines, statements, branches, functions), asserting: case-insensitive registration and lookup resolve `sum`/`Sum`/`SUM` to one implementation and an unregistered name resolves to nothing; the default propagation returns the first error argument in left-to-right order before any coercion; number coercion converts boolean true/false to 1/0, parses a numeric string, treats empty as 0, and yields `#VALUE!` for a non-coercible value; text coercion converts a number and a boolean to text form; range flattening produces the rectangular block, reduces in row-by-row order skipping empty cells, errors `#VALUE!` on a wrong-type value for numeric reducers, and skips non-numbers for a pure counting reducer.
- Negative and edge cases: error arguments at varying positions, an empty argument list, a range containing only empty cells, and a wrong-type value inside a range for both numeric-reducing and counting code paths.
- The deterministic gate `gate:test:registry` resolving to a real green gate-run record (status: pass, exit_code: 0), not a free-text label.

No later code or issue may build on this slice until it is proven fully functional and fully tested here, and completion is claimed only from fresh green evidence on the current tree.
