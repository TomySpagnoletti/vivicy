/**
 * Vivicy brand constants. Defined once, reused everywhere. The product name and
 * tagline are fixed; never tenant-configurable, never database state.
 */
export const BRAND = {
  name: "Vivicy",
  tagline: "visual vibe coding",
} as const
